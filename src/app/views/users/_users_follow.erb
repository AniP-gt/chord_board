<%= link_to user_relationships_path(@user.id),method: :post ,remote: true do %>
  <button class="usershow__container--follow_btn-do">フォローする</button>
<%end %>

<%# <%= form_for(current_user, url: user_relationships_path, method: :post, remote: true) do |f| %> %>
  <%# <%= hidden_field_tag :followed_id, @user.id %> %>
  <%# <%= f.submit "フォローする", class: "btn btn-outline-secondary" %> %>
<%# <% end %> %>


<%# <%= form_with(model: current_user.active_relationships.build, local: false) do |f| %> %>
  <%# <div><%= hidden_field_tag :followed_id, @user.id %></div> %>
  <%# <%= f.submit "フォローする", class: "btn btn-primary" %> %>
<%# <% end %> %>