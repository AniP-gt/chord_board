<%= link_to user_relationships_path(@user.id),method: :delete ,remote: true do %>
  <button class="usershow__container--follow_btn-remove">フォローを外す</button>
<%end %>

<%# <%= form_for(current_user, url: user_relationship_path(@user), method: :delete, remote: true) do |f| %> %>
  <%# <%= f.submit "フォロー解除", class: "btn btn-outline-secondary" %> %>
<%# <% end %> %>


<%# <%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, local: false) do |f| %> %>
  <%# <%= f.submit "フォロー解除", class: "btn btn-default" %> %>
<%# <% end %> %>